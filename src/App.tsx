import { Landing } from '@homework-task/components/landing/Landing';
import './styles.css';

const App: React.FC = () => {
    return (
        <main>
            <Landing />
        </main>
    );
};

export default App;
